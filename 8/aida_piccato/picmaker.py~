def picmaker():
    headers = "P3 100 100 255"
    pixels = ""
    for(int i = 0; i < 100; i++):
        for(int j = 0; j < 100; j++):
            pixels += "(%d, %d, %d)" % (i, i, i)
    f = open("pic.ppm", 'w')
    f.truncate()
    f.write(headers)
    f.write(pixels)
    f.close()

picmaker()
